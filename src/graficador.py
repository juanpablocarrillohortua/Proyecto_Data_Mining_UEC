import pandas as pd
import seaborn as sns
import matplotlib.pyplot as plt

class Graficador:

    def __init__(self, ruta_guardado):
        self.ruta_guardado = ruta_guardado

    def barplot_discreto(self, data_frame, var, legend, titulo, etiqueta_x, etiqueta_y, count_label):
        pass